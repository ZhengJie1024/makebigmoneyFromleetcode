#include "1155.h"
#include <math.h>
#include <iostream>
#include <vector>

// nn train
int main() {
  Solution s;
  std::vector<int> stones = {1,2,5};
  int res = s.numRollsToTarget( 2,6,7);

  int i = (int) ((((unsigned int) 1) << 31) - 1);
  std::cout << " res = done " << i << " " << INT_MAX ;
  return 0;
}

